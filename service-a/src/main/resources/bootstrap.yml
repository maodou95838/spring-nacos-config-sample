spring:
  application:
    name: nacos-config-sample-service-a

  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        prefix: example
        namespace: dev
        file-extension: yml